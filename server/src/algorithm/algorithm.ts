import {Users} from "../entities/Users";

const MealPlanAlgorithm = (user: Users, budget, timeLimit) => {
    // const recipecesByDiet = getRecipesByDiet(user.restrictions);
    // const recipecesByRestrictions = getRecipesByRestrictions(user.diets);
    // const r1 = new Set(recipecesByDiet);
    // const r2 = new Set(recipecesByRestrictions);
    // const resultRecipeSet = [];
    // for(let rec of r1){
    //     if(r2.has(rec)){
    //         resultRecipeSet.push(rec);
    //     }
    // }
    // const priceOnDay = budget / timeLimit;
    // resultRecipeSet.filter((x)=>x.price)
    // resultRecipeSet.sort(() => Math.random() - 0.5);


}

export default MealPlanAlgorithm;